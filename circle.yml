machine:
  node:
    version: 0.10.22
dependencies:
  post:
    - ../node_modules/.bin/mrt install:
        pwd: app
test:
  post:
    - "mv coverage $CIRCLE_ARTIFACTS"
    - "mv coverage-browser $CIRCLE_ARTIFACTS"
